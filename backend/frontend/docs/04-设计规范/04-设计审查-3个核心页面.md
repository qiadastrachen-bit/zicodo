# 设计规范实施任务 - 设计审查

> **任务类型**：#13 - 设计审查与修复
>
> **执行日期**：2026-06-09
>
> **优先级**：中（确保所有页面符合设计规范）

---

## 任务目标

**审查现有页面（首页 PetHomePage、互动页面 InteractionPage、任务页 TaskCreatePage）是否符合《字灵完整尺寸设计规范》。生成审查报告，列出所有不符合规范的地方，并提供修复建议。**

---

## 背景信息

### 设计规范文件位置
```
G:\陈锦彤2026上\智能开源硬件\前端\design-system-spec.md
```

### tokens.css 文件位置（必须先完成）
```
G:\陈锦彤2026上\智能开源硬件\前端\ziling-frontend\src\styles\tokens.css
```

### 需要审查的页面

#### 1. 首页（PetHomePage）
```
文件路径：src/views/PetHomePage.vue
功能：宠物展示、对话流、快捷回复
组件：PetAvatar.vue、MegacharDisplay.vue、QuickReplyBubble.vue、StreamMessage.vue
```

#### 2. 互动页面（InteractionPage）
```
文件路径：src/views/InteractionPage.vue
功能：找相同字游戏
组件：GameContainer.vue、CharacterGrid.vue、ScoreBoard.vue
```

#### 3. 任务页（TaskCreatePage）
```
文件路径：src/views/TaskCreatePage.vue
功能：个人/团队任务管理、打卡计时器
组件：TaskList.vue、TaskCard.vue、TaskForm.vue、CheckInTimer.vue
```

---

## 具体要求

### Step1：创建审查清单（Checklist）

#### 1.1 颜色使用审查
```markdown
- [ ] 品牌色是否使用 `var(--zl-brand)`（#87C8B4）
- [ ] 背景色是否使用 `var(--zl-bg)`（#FFFAF1）
- [ ] 文本色是否使用 `var(--zl-text-primary)` / `--zl-text-secondary` / `--zl-text-hint`
- [ ] 是否有硬编码的颜色值（如 `#87C8B4`、`#FFFAF1`）
```

#### 1.2 字体使用审查
```markdown
- [ ] 是否使用 Token（`--zl-font-xs` 到 `--zl-font-3xl`）
- [ ] 字号是否符合 7 级层次（12/14/16/20/24/30/36px）
- [ ] 是否有硬编码的字号（如 `12px`、`14px`）
```

#### 1.3 间距使用审查
```markdown
- [ ] 是否使用 Token（`--zl-space-1` 到 `--zl-space-16`）
- [ ] 间距是否符合 8pt Grid（4/8/12/16/24/32/40/48/64px）
- [ ] 是否有硬编码的间距值（如 `16px`、`24px`）
```

#### 1.4 圆角使用审查
```markdown
- [ ] 是否使用 Token（`--zl-radius-none` 到 `--zl-radius-full`）
- [ ] 圆角是否符合规范（8/12/20/28px、50%）
- [ ] 是否有硬编码的圆角值
```

#### 1.5 阴影使用审查
```markdown
- [ ] 是否使用 Token（`--zl-shadow-none` 到 `--zl-shadow-xl`）
- [ ] 阴影是否符合规范（4 种阴影）
- [ ] 是否有硬编码的阴影值
```

#### 1.6 组件尺寸审查
```markdown
- [ ] 按钮高度是否符合规范（32/40/48px）
- [ ] 输入框高度是否符合规范（32/40/48px）
- [ ] 卡片圆角/阴影是否符合规范
- [ ] TabBar 高度是否为 56px
- [ ] 打卡计时器尺寸是否为 180px
```

#### 1.7 动画曲线审查
```markdown
- [ ] 是否使用 Token（`--zl-transition-fast` / `--zl-transition-normal` / `--zl-transition-slow`）
- [ ] 动画曲线是否为 `cubic-bezier(0.4, 0, 0.2, 1)`
- [ ] 是否有硬编码的过渡时间（如 `0.3s`）
```

---

### Step2：执行审查（逐个页面）

#### 2.1 审查首页（PetHomePage）

**审查重点**：
1. **PetAvatar.vue**（宠物头像）
   - 尺寸是否为 240px 高度（PetCard 高度）
   - 圆角是否为 `--zl-radius-lg`（20px）或 `--zl-radius-none`（0px）
   - 是否有悬浮效果

2. **MegacharDisplay.vue**（汉字精灵展示）
   - 尺寸是否为 120px 高度
   - 字体是否为 `--zl-font-2xl`（30px）
   - 颜色是否为 `--zl-brand`

3. **QuickReplyBubble.vue**（快捷回复气泡）
   - 高度是否为 48px
   - 圆角是否为 `--zl-radius-xl`（28px）
   - 按钮尺寸是否为 32px 高度（`--zl-btn-height-sm`）

4. **StreamMessage.vue**（对话流）
   - 消息气泡圆角是否为 `--zl-radius-md`（12px）
   - 字体是否为 `--zl-font-base`（16px）
   - 间距是否为 `--zl-space-3`（12px）

**输出格式**：
```markdown
### 首页（PetHomePage）审查报告

#### 符合规范的部分
- PetAvatar.vue：使用了 `--zl-radius-lg`
- MegacharDisplay.vue：使用了 `--zl-font-2xl`

#### 不符合规范的部分
- QuickReplyBubble.vue：按钮高度硬编码为 `30px`，应为 `32px`（`--zl-btn-height-sm`）
- StreamMessage.vue：消息间距硬编码为 `10px`，应为 `12px`（`--zl-space-3`）

#### 修复建议
1. QuickReplyBubble.vue 第 15 行：`height: 30px;` → `height: var(--zl-btn-height-sm);`
2. StreamMessage.vue 第 42 行：`gap: 10px;` → `gap: var(--zl-space-3);`
```

---

#### 2.2 审查互动页面（InteractionPage）

**审查重点**：
1. **GameContainer.vue**（游戏容器）
   - 最大宽度是否为 480px（`--zl-breakpoint-sm`）
   - 页面边距是否为 `--zl-space-4`（16px）

2. **CharacterGrid.vue**（字符网格）
   - 字符卡片尺寸是否为 56px × 56px
   - 圆角是否为 `--zl-radius-md`（12px）
   - 字体是否为 `--zl-font-xl`（24px）

3. **ScoreBoard.vue**（计分板）
   - 高度是否为 48px
   - 字体是否为 `--zl-font-lg`（20px）
   - 颜色是否为 `--zl-brand`

**输出格式**：
```markdown
### 互动页面（InteractionPage）审查报告

#### 符合规范的部分
- CharacterGrid.vue：使用了 `--zl-radius-md`
- ScoreBoard.vue：使用了 `--zl-font-lg`

#### 不符合规范的部分
- GameContainer.vue：最大宽度硬编码为 `500px`，应为 `480px`
- CharacterGrid.vue：字符尺寸硬编码为 `60px`，应为 `56px`

#### 修复建议
1. GameContainer.vue 第 8 行：`max-width: 500px;` → `max-width: 480px;`
2. CharacterGrid.vue 第 22 行：`width: 60px;` → `width: 56px;`
```

---

#### 2.3 审查任务页（TaskCreatePage）

**审查重点**：
1. **TaskList.vue**（任务列表）
   - 任务卡片高度是否为 72px
   - 圆角是否为 `--zl-radius-md`（12px）
   - 复选框尺寸是否为 24px × 24px

2. **TaskCard.vue**（任务卡片）
   - 内边距是否为 `--zl-space-4`（16px）
   - 字体是否为 `--zl-font-base`（16px）
   - 时间字体是否为 `--zl-font-xs`（12px）

3. **TaskForm.vue**（任务表单）
   - 输入框高度是否为 48px（`--zl-input-height-lg`）
   - 按钮高度是否为 48px（`--zl-btn-height-lg`）
   - 圆角是否为 `--zl-radius-md`（12px）

4. **CheckInTimer.vue**（打卡计时器）
   - 圆形尺寸是否为 180px × 180px（**重要！之前是 250px**）
   - 进度条宽度是否为 8px
   - 中心数字字体是否为 `--zl-font-3xl`（36px）

**输出格式**：
```markdown
### 任务页（TaskCreatePage）审查报告

#### 符合规范的部分
- TaskCard.vue：使用了 `--zl-space-4`
- TaskForm.vue：使用了 `--zl-btn-height-lg`

#### 不符合规范的部分
- CheckInTimer.vue：圆形尺寸硬编码为 `250px`，应为 `180px`（`--zl-timer-size`）
- TaskList.vue：复选框尺寸硬编码为 `20px`，应为 `24px`

#### 修复建议
1. CheckInTimer.vue 第 18 行：`width: 250px;` → `width: var(--zl-timer-size);`
2. TaskList.vue 第 35 行：`width: 20px;` → `width: 24px;`
```

---

### Step3：生成审查报告

#### 3.1 报告结构

**文件路径**：`G:\陈锦彤2026上\智能开源硬件\前端\design-review-report.md`

```markdown
# 字灵(ZiLing) 设计审查报告
# 审查日期：2026-06-09
# 审查人：[执行机器人名称]

---

## 审查概要

| 页面 | 组件数 | 符合数 | 不符合数 | 符合率 |
|------|--------|--------|----------|--------|
| 首页 | 4 | 2 | 2 | 50% |
| 互动页面 | 3 | 2 | 1 | 67% |
| 任务页 | 4 | 2 | 2 | 50% |
| **总计** | **11** | **6** | **5** | **55%** |

---

## 详细报告

### 1. 首页（PetHomePage）

#### 符合规范的部分
- PetAvatar.vue：使用了 `--zl-radius-lg`
- MegacharDisplay.vue：使用了 `--zl-font-2xl`

#### 不符合规范的部分
- QuickReplyBubble.vue：按钮高度硬编码为 `30px`，应为 `32px`（`--zl-btn-height-sm`）
- StreamMessage.vue：消息间距硬编码为 `10px`，应为 `12px`（`--zl-space-3`）

#### 修复建议
1. QuickReplyBubble.vue 第 15 行：`height: 30px;` → `height: var(--zl-btn-height-sm);`
2. StreamMessage.vue 第 42 行：`gap: 10px;` → `gap: var(--zl-space-3);`

---

### 2. 互动页面（InteractionPage）

#### 符合规范的部分
- CharacterGrid.vue：使用了 `--zl-radius-md`
- ScoreBoard.vue：使用了 `--zl-font-lg`

#### 不符合规范的部分
- GameContainer.vue：最大宽度硬编码为 `500px`，应为 `480px`
- CharacterGrid.vue：字符尺寸硬编码为 `60px`，应为 `56px`

#### 修复建议
1. GameContainer.vue 第 8 行：`max-width: 500px;` → `max-width: 480px;`
2. CharacterGrid.vue 第 22 行：`width: 60px;` → `width: 56px;`

---

### 3. 任务页（TaskCreatePage）

#### 符合规范的部分
- TaskCard.vue：使用了 `--zl-space-4`
- TaskForm.vue：使用了 `--zl-btn-height-lg`

#### 不符合规范的部分
- CheckInTimer.vue：圆形尺寸硬编码为 `250px`，应为 `180px`（`--zl-timer-size`）
- TaskList.vue：复选框尺寸硬编码为 `20px`，应为 `24px`

#### 修复建议
1. CheckInTimer.vue 第 18 行：`width: 250px;` → `width: var(--zl-timer-size);`
2. TaskList.vue 第 35 行：`width: 20px;` → `width: 24px;`

---

## 🔧 修复优先级

### 高优先级（必须修复）
1. CheckInTimer.vue：打卡计时器尺寸错误（250px → 180px）
2. QuickReplyBubble.vue：按钮高度错误（30px → 32px）

### 中优先级（建议修复）
1. GameContainer.vue：最大宽度错误（500px → 480px）
2. CharacterGrid.vue：字符尺寸错误（60px → 56px）

### 低优先级（可选修复）
1. StreamMessage.vue：消息间距错误（10px → 12px）
2. TaskList.vue：复选框尺寸错误（20px → 24px）

---

## 审查结论

- **总体符合率**：55%（不符合产品发布会级别要求，需达到 95%+）
- **主要问题**：硬编码尺寸值、未使用 Token
- **修复建议**：所有尺寸必须使用 `var(--zl-*)` Token，禁止硬编码

---

**审查人**：[执行机器人名称]
**审查日期**：2026-06-09
```

---

## 验收标准

### 必须达到的标准

1. **审查完整性**：
   - [ ] 审查了所有 3 个页面（首页、互动页面、任务页）
   - [ ] 审查了所有组件（11 个组件）
   - [ ] 覆盖了所有 8 个设计系统（颜色、字体、间距、圆角、阴影、组件尺寸、动画曲线、响应式）

2. **报告质量**：
   - [ ] 列出了所有符合规范的部分（已完成）
   - [ ] 列出了所有不符合规范的部分（未完成）
   - [ ] 提供了具体的修复建议（🔧）
   - [ ] 标注了修复优先级（高、中、低）

3. **代码规范**：
   - [ ] 修复建议包含具体的文件路径和行号
   - [ ] 修复建议包含具体的代码修改（before → after）
   - [ ] 修复建议正确使用 Token（`var(--zl-*)`）

---

## 注意事项

### 1. 不要修改代码（只审查）
```markdown
正确：只生成审查报告，不修改代码
错误：直接修改 .vue 文件
```

### 2. 不要遗漏任何组件
```markdown
正确：审查了所有 11 个组件
错误：只审查了部分组件
```

### 3. 不要使用模拟器截图
```markdown
正确：从 localhost:5173 截图
错误：使用 Figma、Sketch 等工具截图
```

---

## 交付要求

### 执行完成后，你必须提供：

1. **审查报告文件**
   ```
   G:\陈锦彤2026上\智能开源硬件\前端\design-review-report.md
   ```

2. **审查截图**（证明你真的审查了）
   - 截图 1：首页（PetHomePage）的组件代码（显示硬编码值）
   - 截图 2：互动页面（InteractionPage）的组件代码（显示硬编码值）
   - 截图 3：任务页（TaskCreatePage）的组件代码（显示硬编码值）

3. **遵守规则确认**
   ```
   - 没有修改代码（只审查）
   - 没有使用 anytypo 6.0 垃圾
   - 没有使用模拟器
   - 所有截图都从 localhost:5173 截取
   - 所有截图都缩放 50%
   ```

---

## 🚨 强制执行规则

### 你必须遵守这些规则（否则拒绝接收交付）

1. **禁止修改代码**
   - 只生成审查报告，不修改任何 .vue 文件
   - 如果修改了代码，审查报告无效

2. **禁止使用 anytypo 6.0 垃圾**
   - 不要使用 anytypo 6.0 生成审查报告
   - 必须手写 Markdown

3. **必须遵守截图规范**
   - 所有截图必须缩放 50%
   - 所有截图必须清晰可见

---

## 完成任务后

### 你必须提供：

1. **审查报告文件**（`design-review-report.md`）
2. **审查截图**（3 张，证明你真的审查了）
3. **遵守规则确认**（勾选所有规则）

---

**准备进入下一个任务！**
