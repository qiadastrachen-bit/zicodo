# +页面/日程任务页决定（2026-06-08）

## 关键决策（已确认）

### 1. 页面定位
- **路由**：`/add`（TabBar 第3个图标，+ 特殊按钮）
- **文件**：`src/pages/tasks/TaskCreatePage.vue`（重写已有占位文件）
- **功能**：个人日程任务管理（列表 + 新建 + 编辑 + 打卡计时器）

### 2. 数据存储方案
- **选择**：localStorage（MVP 最快实现）
- **Key**：`ziling_schedule_tasks`
- **原因**：
  -  deadline 紧迫（周二晚部署）
  -  现有 task.js store 是家长-孩子任务系统，数据模型不匹配
  -  日程任务独立存储，不与现有系统冲突
- **状态**：✅ 已确认

### 3. 个人 vs 团队
- **个人**：完整功能（列表/新建/编辑/打卡）
- **团队**：UI 占位（点击显示"即将上线"）
- **原因**：MVP 先完成个人部分
- **状态**：✅ 已确认

### 4. 闹铃功能
- **实现**：仅视觉 Toggle 开关
- **不实现**：实际推送通知（超出 MVP 范围）
- **状态**：✅ 已确认

### 5. 打卡计时器
- **实现**：前台圆形进度条 + 线性进度条
- **不实现**：后台运行/Service Worker
- **状态**：✅ 已确认

### 6. 自动分类逻辑
- **今日任务**：日期为今天，或重复规则包含今天
- **本周/本月任务**：日期在本月内但不是今天
- **其他**：重复任务（非今天），或未来任务
- **状态**：✅ 已确认

### 7. 组件架构
```
TaskCreatePage.vue（主页面）
├── ScheduleHeader.vue（顶部栏）
├── TaskSection.vue（分区标题）
├── TaskCard.vue（任务卡片）
├── TaskForm.vue（新建/编辑弹窗）
├── CheckInTimer.vue（打卡计时器）
└── ToggleSwitch.vue（通用开关）
```

### 8. 路由修改
- 仅修改：`/add` 路由的 `meta.showTabBar` 设为 `true`
- TabBar 元素：保持固定（5个图标，不允许修改）
- **状态**：✅ 已确认

---

## 交互决定（2026-06-08 04:08 补充）

### 1. 导航栏交互
- **点击 + 图标**：跳转到 `/add` 页面（日程任务页）
- **当前页就是 /add**：+ 图标高亮（active 状态）
- **路由配置**：✅ 已修正（`meta.showTabBar: true`）
- **状态**：✅ 已确认，点击 + 能回到这里

### 2. 个人/团队 Tab 切换
- **交互方式**：顶部两个 Tab（个人/团队），点击切换
- **个人页**：完整功能（列表/新建/编辑/打卡）
- **团队页**：UI 占位（"即将上线"）
- **对称性**：团队任务的交互方式**可能与个人页面对称**（待确认）
- **状态**：⏳ 个人页已确认，团队页待实现

### 3. 任务卡片交互
- **点击卡片**：进入任务详情/编辑模式
- **点击复选框**：标记任务完成（视觉反馈 + 数据更新）
- **滑动卡片**（可选）：显示"删除"按钮
- **状态**：⏳ 待实现

### 4. 新建/编辑弹窗交互
- **点击 + 按钮**：弹出新建任务表单
- **点击卡片**：弹出编辑任务表单（预填充数据）
- **点击遮罩/取消**：关闭弹窗
- **点击确认**：保存任务 + 关闭弹窗 + 刷新列表
- **表单验证**：日期、时间、标题必填
- **状态**：⏳ 待实现

### 5. 打卡计时器交互
- **点击开始**：启动计时器（圆形进度条 + 数字跳动）
- **点击完成**：标记打卡完成 + 显示庆祝动画
- **退出页面**：计时器暂停（不销毁，keep-alive 缓存）
- **返回页面**：计时器恢复（从缓存中读取状态）
- **计划时长显示**：
  - 有 `startTime` + `endTime` → 计算时长并显示（如"60 min"）
  - 无 `endTime` → 显示"进行中" ← **2026-06-08 04:15 确认**
- **状态**：⏳ 待实现

### 6. 其他交互（待确认）
- **用户说"这一页其实还有其它的交互东西在"**
- **具体内容**：⏳ 待用户确认（需要你告诉我具体是什么交互）
- **下一步**：先做已知交互，再补充其他交互

---

## 技术架构

### 数据流
```
用户操作 → Schedule Store (Pinia) → localStorage
                           ↓
                     自动分类计算（today/month/other）
                           ↓
                     Vue 响应式更新 UI
```

### 关键文件
| 文件 | 说明 |
|------|------|
| `src/stores/schedule.js` | localStorage 读写 + 分类计算 |
| `src/components/schedule/TaskForm.vue` | 新建/编辑共享组件 |
| `src/components/schedule/CheckInTimer.vue` | SVG 圆形进度条 + 计时器 |

---

**保存时间：2026-06-08 03:55**
